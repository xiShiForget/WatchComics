/**
 * Created by 178063117 on 2017/10/17.
 */
import * as types from './mutationsType'
export const getLike = ({commit}, today) => {
  // commit(types.RECEIVE_TODAY_LIKES,today)
  // commit(types.RECEIVE_TODAY_COMMENTS,today)
  // commit(types.ADD_TO_LIKE,today)
  // console.log(today,'22222')
}
